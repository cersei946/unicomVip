
/**
* wap端-注册页面
* author：wangbiguo
* */
$(function (){
    var u = navigator.userAgent;
	//基础数据
    var baseInfo = {
        
    };
    //参数
    var PAPM = {
 
    };

    
    function DownLoad (){}

    
    var downLoad = new DownLoad();
    var init = {
       downloadFn: function (){
            
       }
    };
    // 初始化
    $.each(init,function (k){
        if (k) {
            init[k]();
        }
    });
    
});